# Conceptual production Terraform
#
# Modules here must remain documentation-oriented and must NOT auto-create
# chargeable AWS resources. See docs/deployment.md production mapping.
#
# Intentionally empty of live providers until an operator opts in.
